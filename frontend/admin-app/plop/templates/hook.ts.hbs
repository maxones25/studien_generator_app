
export interface Use{{pascalCase name}}Options {}

export interface Use{{pascalCase name}}Result {}

export const use{{pascalCase name}} = (options? : Use{{pascalCase name}}Options) : Use{{pascalCase name}}Result => {
    return {
        
    }
}