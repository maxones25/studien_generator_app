import {} from '@entities/schema';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { I{{pascalCase name}} } from '@admin/{{pascalCase module}}/domain';
import { TypeOrmRepository } from '@shared/modules/db';

export class {{pascalCase name}}
  extends TypeOrmRepository<{{pascalCase module}}Schema>
  implements IDirectorsRepository
{
  constructor(
    @InjectRepository({{pascalCase module}}Schema)
    db: Repository<{{pascalCase module}}Schema>,
  ) {
    super(db);
  }
}
